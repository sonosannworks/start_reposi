<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>理想のセカンドライフについてのアンケート</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@300;400;500;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #b4a078;
            --text-color: #333;
            --bg-color: #f9f8f6;
            --border-color: #e8e0d0;
            --error-color: #d32f2f;
            --success-color: #388e3c;
        }

        body {
            font-family: 'Noto Sans JP', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            line-height: 1.6;
            margin: 0;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .container {
            background-color: #fff;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.05);
            max-width: 600px;
            width: 100%;
        }

        /* スレッド内容の表示エリア */
        .thread-content {
            background-color: #faf8f4;
            padding: 20px 25px;
            border-left: 4px solid var(--primary-color);
            border-radius: 0 8px 8px 0;
            margin-bottom: 30px;
            font-size: 0.95rem;
            color: #4a4540;
        }

        .thread-content p {
            margin-top: 0;
            margin-bottom: 1em;
        }
        .thread-content p:last-child {
            margin-bottom: 0;
            font-weight: 500;
        }

        h2 {
            text-align: center;
            font-weight: 500;
            color: #1a1a2e;
            margin-top: 0;
            margin-bottom: 30px;
            font-size: 1.5rem;
        }

        .form-group {
            margin-bottom: 25px;
        }

        label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
            font-size: 0.95rem;
            color: #444;
        }

        .required {
            color: var(--error-color);
            font-size: 0.8em;
            margin-left: 4px;
        }

        input[type="text"],
        select,
        textarea {
            width: 100%;
            padding: 12px;
            border: 1px solid var(--border-color);
            border-radius: 6px;
            box-sizing: border-box;
            font-family: inherit;
            font-size: 1rem;
            transition: border-color 0.3s ease;
        }

        input[type="text"]:focus,
        select:focus,
        textarea:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 2px rgba(180, 160, 120, 0.2);
        }

        textarea {
            resize: vertical;
            min-height: 120px;
        }

        /* ボタン風チェックボックスのスタイル */
        .checkbox-group {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }

        .checkbox-label {
            cursor: pointer;
            position: relative;
            margin: 0;
            display: inline-block;
        }

        .checkbox-label input[type="checkbox"] {
            position: absolute;
            opacity: 0;
            width: 0;
            height: 0;
        }

        .checkbox-text {
            display: inline-block;
            padding: 8px 16px;
            border: 1px solid var(--border-color);
            border-radius: 20px;
            background-color: #fff;
            color: #666;
            font-size: 0.9rem;
            transition: all 0.2s ease;
            user-select: none;
        }

        .checkbox-label:hover .checkbox-text {
            background-color: #fdfbf7;
            border-color: #d4c4a8;
        }

        /* チェックされた時のスタイル */
        .checkbox-label input[type="checkbox"]:checked + .checkbox-text {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
            color: #fff;
            font-weight: 500;
        }

        /* 送信ボタン */
        .submit-btn {
            background-color: var(--primary-color);
            color: white;
            border: none;
            padding: 15px;
            width: 100%;
            border-radius: 6px;
            font-size: 1.1rem;
            font-weight: 500;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.1s ease;
            margin-top: 10px;
        }

        .submit-btn:hover {
            background-color: #a38f67;
        }

        .submit-btn:active {
            transform: scale(0.98);
        }

        .submit-btn:disabled {
            background-color: #ccc;
            cursor: not-allowed;
            transform: none;
        }

        /* ローディングインジケーター */
        .loading {
            display: none;
            text-align: center;
            margin-top: 20px;
        }

        .spinner {
            border: 4px solid rgba(0, 0, 0, 0.1);
            width: 36px;
            height: 36px;
            border-radius: 50%;
            border-left-color: var(--primary-color);
            animation: spin 1s linear infinite;
            margin: 0 auto;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* メッセージエリア */
        #messageArea {
            display: none;
            margin-top: 20px;
            padding: 15px;
            border-radius: 6px;
            text-align: center;
            font-weight: 500;
        }

        .success {
            background-color: #e8f5e9;
            color: var(--success-color);
            border: 1px solid #c8e6c9;
        }

        .error {
            background-color: #ffebee;
            color: var(--error-color);
            border: 1px solid #ffcdd2;
        }

        @media (max-width: 480px) {
            .container {
                padding: 25px 20px;
            }
            .checkbox-text {
                font-size: 0.85rem;
                padding: 8px 12px;
            }
        }
    </style>
</head>
<body>

    <div class="container">
        <h2>アンケートにご協力ください</h2>
        
        <div class="thread-content">
            <p>介護保険は、損得勘定で使うものではなく、いざという時に自分や家族の生活を守るための「お守り」です。</p>
            <p>誰だって、好んで介護が必要な状態になりたいわけではありませんよね。<br>私たちはみんな、平等に歳を重ねていきます。</p>
            <p>50代、60代、その先へ――。<br>お金の仕組みを賢く理解した上で、あなた自身はこれから「どんな年の取り方」をしていきたいですか？</p>
            <p>健康で自立した毎日を長く続けることこそが、実は一番の「得」なのかもしれません。<br>あなたの理想のセカンドライフについて、ぜひコメントで教えてください！</p>
        </div>

        <form id="surveyForm">
            <div class="form-group">
                <label for="name">お名前 (ニックネーム可)<span class="required">*</span></label>
                <input type="text" id="name" name="name" required placeholder="例: さつき">
            </div>

            <div class="form-group">
                <label for="age">ご年代<span class="required">*</span></label>
                <select id="age" name="age" required>
                    <option value="" disabled selected>選択してください</option>
                    <option value="20代以下">20代以下</option>
                    <option value="30代">30代</option>
                    <option value="40代">40代</option>
                    <option value="50代">50代</option>
                    <option value="60代">60代</option>
                    <option value="70代">70代</option>
                    <option value="80代以上">80代以上</option>
                </select>
            </div>

            <div class="form-group">
                <label>現在の状況・気になっていること (複数選択可)</label>
                <div class="checkbox-group">
                    <label class="checkbox-label">
                        <input type="checkbox" name="concerns" value="先のことは考えていない">
                        <span class="checkbox-text">① 先のことは考えていない</span>
                    </label>
                    <label class="checkbox-label">
                        <input type="checkbox" name="concerns" value="今親が介護保険サービスを利用している">
                        <span class="checkbox-text">② 今親が介護保険サービスを利用している</span>
                    </label>
                    <label class="checkbox-label">
                        <input type="checkbox" name="concerns" value="親が入院している">
                        <span class="checkbox-text">③ 親が入院している</span>
                    </label>
                    <label class="checkbox-label">
                        <input type="checkbox" name="concerns" value="親の介護に不安がある">
                        <span class="checkbox-text">④ 親の介護に不安がある</span>
                    </label>
                    <label class="checkbox-label">
                        <input type="checkbox" name="concerns" value="親の物忘れが気になる">
                        <span class="checkbox-text">⑤ 親の物忘れが気になる</span>
                    </label>
                    <label class="checkbox-label">
                        <input type="checkbox" name="concerns" value="自分の未来のため何ができるのか知りたい">
                        <span class="checkbox-text">⑥ 自分の未来のため何ができるのか知りたい</span>
                    </label>
                </div>
            </div>

            <div class="form-group">
                <label for="comment">あなたの理想のセカンドライフを教えてください<span class="required">*</span></label>
                <textarea id="comment" name="comment" required placeholder="健康で旅行にたくさん行きたい、趣味を充実させたい、など自由にお書きください。"></textarea>
            </div>

            <button type="submit" class="submit-btn" id="submitBtn">送信する</button>
            
            <div class="loading" id="loadingIndicator">
                <div class="spinner"></div>
                <p style="margin-top: 10px; color: #666; font-size: 14px;">送信中...</p>
            </div>

            <div id="messageArea"></div>
        </form>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const form = document.getElementById('surveyForm');
            const submitBtn = document.getElementById('submitBtn');
            const messageArea = document.getElementById('messageArea');
            const loadingIndicator = document.getElementById('loadingIndicator');

            // ★画像から読み取ったあなたのデプロイURLが設定されています★
            const GAS_URL = 'https://script.google.com/macros/s/AKfycbwE4ME1TGD87cafgTA9AFKdimc38p0xTDfxGYMmrkBsQ2RCu8X4C_epFeprO5aRFNk/exec';

            form.addEventListener('submit', async (e) => {
                e.preventDefault();

                // フォームデータの取得
                const formData = new FormData(form);
                
                // チェックボックスの値を配列にまとめる
                const selectedConcerns = [];
                formData.getAll('concerns').forEach(value => {
                    selectedConcerns.push(value);
                });

                const data = {
                    name: formData.get('name'),
                    age: formData.get('age'),
                    concerns: selectedConcerns.join(', '), // カンマ区切りの文字列にして送信
                    comment: formData.get('comment'),
                    timestamp: new Date().toISOString()
                };

                // UIの状態を「送信中」に変更
                submitBtn.disabled = true;
                submitBtn.style.display = 'none';
                loadingIndicator.style.display = 'block';
                messageArea.style.display = 'none';
                messageArea.className = '';

                try {
                    // Fetch APIを使用してGASへPOSTリクエストを送信 (CORS対策で text/plain)
                    const response = await fetch(GAS_URL, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'text/plain',
                        },
                        body: JSON.stringify(data)
                    });

                    // 成功時の処理
                    if (response.ok) {
                        form.reset();
                        // 選択されたチェックボックスの見た目もリセットするために、ラジオ/チェックボックスの再描画を促す
                        const checkboxes = form.querySelectorAll('input[type="checkbox"]');
                        checkboxes.forEach(cb => cb.checked = false);

                        showMessage('アンケートへのご協力ありがとうございました！', 'success');
                    } else {
                        throw new Error('ネットワークエラーが発生しました。');
                    }
                } catch (error) {
                    // エラー時の処理
                    console.error('Error:', error);
                    showMessage('送信に失敗しました。時間をおいて再度お試しください。', 'error');
                } finally {
                    // UIの状態を元に戻す
                    submitBtn.disabled = false;
                    submitBtn.style.display = 'block';
                    loadingIndicator.style.display = 'none';
                }
            });

            // メッセージ表示用のヘルパー関数
            function showMessage(text, type) {
                messageArea.textContent = text;
                messageArea.className = type;
                messageArea.style.display = 'block';
            }
        });
    </script>
</body>
</html>